#!/usr/bin/env bash

ACTION=$(yad --entry --title "Shutdown menu" \
--width 300 \
--image gnome-shutdown \
--button=gtk-ok:0 \
--button=gtk-close:1 \
--text "Choose:" \
--entry-text \
"Logout" "Reboot" "Power Off"
)
ret=$?

[ "$ret" == "1" ] && exit 0

case $ACTION in
    Logout*)
        bspc quit
        ;;
    Reboot*)
        systemctl reboot
        ;;
    Power*)
        systemctl shutdown
        ;;
esac
