#
# wm independent hotkeys
#

# make sxhkd reload its configuration files:
super + Escape
	pkill -USR1 -x sxhkd

# terminal emulator
super + Return
    $TERMINAL
# program launcher
super + p 
	dmenu_run_colors

alt + 0
    locker

super + {_,shift +} d
    {start_containers,stop_containers}

alt + b
    $BROWSER

super + e
    $TERMINAL -t Ranger -e explorer

alt + s
    rofi -show ssh
alt + w
    rofi -show window
